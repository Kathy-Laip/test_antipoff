import React from "react";
import { SignUpCard } from "../components/SignUpCard";

export const SignUp: React.FC = () => {
    return (
        <main>
            <SignUpCard/>
        </main>
    )
}